---
export const prerender = false;
import Layout from '../../layouts/Layout.astro';
import StudyView from '../../components/StudyView.tsx';
const { subtopicId } = Astro.params;
---
<Layout title="Study - Study Pal" fullWidth={true}>
  <StudyView subtopicId={subtopicId!} client:load />
</Layout>
