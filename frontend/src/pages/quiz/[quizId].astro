---
export const prerender = false;
import Layout from '../../layouts/Layout.astro';
import QuizView from '../../components/QuizView.tsx';
const { quizId } = Astro.params;
---
<Layout title="Quiz - Cortex">
  <QuizView quizId={quizId!} client:load />
</Layout>
